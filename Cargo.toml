[package]
name = "confcosmos"
version = "0.1.0"
edition = "2021"
description = "A TUI tool for quickly creating symlinks, Linux only"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
toml = "0.8"
serde = { version = "1", features = ["derive"] }
chrono = "0.4"
